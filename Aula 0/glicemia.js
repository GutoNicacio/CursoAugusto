//nome e glicemia são variáveis com valores fixados no início do código
let nome = "Júnior";
let glicemia = 130;

//usando if (se) para identificar somente saudável, dentro do intervalo, ou não saudável
if (glicemia >= 70 && glicemia <= 99) {
    alert("Está saudável!");
} else {
    alert("Não está saudável!");
}